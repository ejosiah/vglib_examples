#version 460

layout(local_size_x = 8, local_size_y = 8) in;

#include "resolve.glsl"

void main() {
    ivec2 iPos = ivec2(gl_GlobalInvocationID);
    vec3 color = resolve(iPos);
    imageStore(resolve_image, iPos, vec4(color, 1));
}